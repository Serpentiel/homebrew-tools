[tools]
node = "22.16.0"
# trunk launcher; the actual linter CLI version is pinned in .trunk/trunk.yaml
trunk = "1.3.4"
changie = "1.24.2"
# Taskfile publishes releases with it.
gh = "2.98.0"
task = "3.51.1"
