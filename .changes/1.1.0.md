## [1.1.0](https://github.com/Serpentiel/homebrew-tools/releases/tag/v1.1.0) - 2026-06-30

### Added

- Run brew style on the formula and cask in CI.
- Add a release-triggered workflow that opens formula and cask bump pull requests automatically.
- Distribute the companion app as a Homebrew cask (betterglobekey-companion).
- Add Task (Taskfile.dev) as the entry point for development workflows, route CI through it, and pin the toolchain in mise.toml.

### Changed

- `betterglobekey`: bump version
- Switch to trunk.io for linting purposes

### Fixed

- Build the betterglobekey formula with its version stamped, so the daemon reports the released version instead of dev.
- Logo size in `README.md`
